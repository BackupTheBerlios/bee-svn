*   Implement list of runned scripts, and an option if
    seraph should continue from the last script runned
*   Implement `Submit results`
*   Implement `Results server`
*   Implement `Submit test`
*   Add warm-refresh: server is not restarted, but the SUT state
    is restored using protocol commands( IMAP/POP3 ).
    Maybe a sniffing library would be helpfull.

*   Add tests from shtutza
*   Add fop_cp fop_mkdir
*   Implement/use other environment variables
*   run memtrace after each test was runned
